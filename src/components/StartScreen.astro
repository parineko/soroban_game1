---
// StartScreen.astro
// トップ画面：メニューボタン一覧
---

<div class="screen start-screen" data-screen="start">
  <div class="panel">
    <p class="big-copy">こんにちは 👋</p>
    <p class="text">
      かずを よんだり<br>
      くくを おぼえたり<br>
      たのしく そろばんを まなぼう ✨
    </p>
    
    <div class="button-column">
      <button class="btn primary" data-action="go-level" aria-label="そろばんをよむ">
        そろばんを よむ 📖
      </button>
      
      <button class="btn accent" data-action="go-kuku" aria-label="くくをおぼえる">
        くくを おぼえる 🍒
      </button>

      <div class="divider"></div>

      <a href="/TestZero" class="btn" aria-label="そろばんシミュレーター">
        シミュレーター（たて） 📱
      </a>

      <a href="/RealSoroban" class="btn real-mode" aria-label="ほんもののそろばん">
        ほんものの そろばん（よこ） 🧮
      </a>
    </div>
  </div>
</div>

<style>
  .start-screen {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow-y: auto; /* 画面が小さい時にスクロールできるように */
  }

  .panel {
    width: 100%;
    max-width: 500px;
    margin: 0 auto;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .big-copy {
    font-size: 2rem;
    text-align: center;
    margin: 8px 0 20px;
    color: var(--text-main);
    font-weight: bold;
  }

  .text {
    text-align: center;
    line-height: 1.8;
    font-size: 1.2rem;
    color: var(--text-main);
    margin: 0 0 24px;
    font-weight: 500;
  }

  .button-column {
    display: flex;
    flex-direction: column;
    gap: 12px; /* ボタン同士の間隔 */
    align-items: center;
    width: 100%;
    padding-bottom: 20px;
  }

  /* 区切り線 */
  .divider {
    width: 80%;
    height: 2px;
    background-color: #eaddcf;
    margin: 8px 0;
    border-radius: 2px;
  }

  /* ボタン共通設定 */
  .button-column .btn {
    width: 100%; /* 親要素に合わせて広げる */
    max-width: 320px; /* PCなどで広がりすぎないように */
    font-size: 1.1rem;
    padding: 14px 24px;
    text-decoration: none; /* aタグの下線を消す */
    display: inline-flex;
    justify-content: center;
    align-items: center;
    white-space: nowrap;
    border-radius: 16px;
    font-weight: bold;
    cursor: pointer;
    border: none;
    transition: transform 0.1s, box-shadow 0.1s;
  }

  .btn:active {
    transform: translateY(2px);
  }

  /* --- 色設定 --- */

  /* メインボタン（青系） */
  .btn.primary {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    color: white;
    box-shadow: 0 4px 10px rgba(79, 172, 254, 0.3);
  }

  /* 九九ボタン（赤/ピンク系） */
  .btn.accent {
    background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 99%, #fecfef 100%);
    color: #d81b60;
    box-shadow: 0 4px 10px rgba(255, 154, 158, 0.3);
  }

  /* シミュレーター（既存） */
  .btn {
    background: #f0e0c8;
    color: #5d4037;
    border: 2px solid #d4c4b0;
  }

  /* ★新規: リアルそろばんボタン（木目・本格派っぽい色） */
  .btn.real-mode {
    background: #8d6e63; /* 落ち着いた茶色 */
    color: #ffffff;
    border: 2px solid #5d4037;
    box-shadow: 0 4px 0 #5d4037; /* 立体的な影 */
  }
  .btn.real-mode:active {
    box-shadow: none;
    transform: translateY(4px);
  }
</style>