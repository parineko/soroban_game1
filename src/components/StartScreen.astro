---
// StartScreen.astro
// 文言修正：「そろばんをよむ」「くくをおぼえる」
---

<div class="screen start-screen" data-screen="start">
  <div class="panel">
    <p class="big-copy">こんにちは 👋</p>
    <p class="text">
      そろばんの かずを よんで<br>
      こたえを いれてみよう ✨
    </p>
    <div class="button-column">
      <button class="btn primary" data-action="go-level" aria-label="そろばんをよむ">
        そろばんを よむ 📖
      </button>
      
      <button class="btn accent" data-action="go-kuku" aria-label="くくをおぼえる">
        くくを おぼえる 🍒
      </button>
      
      <a href="/TestZero" class="btn" aria-label="そろばんシミュレーター">そろばんシミュレーター 💻</a>
    </div>
  </div>
</div>

<style>
  .start-screen {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .panel {
    width: 100%;
    max-width: 500px;
    margin: 0 auto;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .big-copy {
    font-size: 2rem;
    text-align: center;
    margin: 8px 0 20px;
    color: var(--text-main);
    font-weight: bold;
  }

  .text {
    text-align: center;
    line-height: 1.8;
    font-size: 1.5rem;
    color: var(--text-main);
    margin: 0 0 32px;
    font-weight: 500;
  }

  .button-column {
    display: flex;
    flex-direction: column;
    gap: 16px;
    align-items: center;
    margin-top: 20px;
    width: 100%;
  }

  .button-column .btn {
    width: auto;
    min-width: 260px;
    max-width: 100%;
    font-size: 1.2rem;
    padding: 16px 24px;
    text-decoration: none;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    white-space: nowrap;
    border-radius: 12px;
    font-weight: bold;
    cursor: pointer;
    border: none;
    transition: transform 0.1s;
  }

  .btn:active {
    transform: translateY(2px);
  }

  /* メインボタン（青系） */
  .btn.primary {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    color: white;
    box-shadow: 0 4px 10px rgba(79, 172, 254, 0.3);
  }

  /* 九九ボタン（赤/ピンク系） */
  .btn.accent {
    background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 99%, #fecfef 100%);
    color: #d81b60;
    box-shadow: 0 4px 10px rgba(255, 154, 158, 0.3);
  }

  /* シミュレーターボタン（デフォルト） */
  .btn {
    background: #f0e0c8;
    color: #5d4037;
    border: 2px solid #d4c4b0;
  }
</style>