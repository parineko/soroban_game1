---
// StartScreen.astro
// 「くく」ボタン追加版
---

<div class="screen start-screen" data-screen="start">
  <div class="panel">
    <p class="big-copy">こんにちは 👋</p>
    <p class="text">
      そろばんの かずを よんで<br>
      こたえを いれてみよう ✨
    </p>
    <div class="button-column">
      <button class="btn primary" data-action="go-level" aria-label="はじめる">はじめる 🎈</button>
      <button class="btn accent" data-action="go-kuku" aria-label="くくであそぶ">くく（99）であそぶ 🍒</button>
      
      <a href="/TestZero" class="btn" aria-label="そろばんシミュレーター">そろばんシミュレーター 💻</a>
    </div>
  </div>
</div>

<style>
  .start-screen {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .panel {
    width: 100%;
    max-width: 500px;
    margin: 0 auto;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .big-copy {
    font-size: 2rem;
    text-align: center;
    margin: 8px 0 20px;
    color: var(--text-main);
    font-weight: bold;
  }

  .text {
    text-align: center;
    line-height: 1.8;
    font-size: 1.5rem;
    color: var(--text-main);
    margin: 0 0 32px;
    font-weight: 500;
  }

  .button-column {
    display: flex;
    flex-direction: column;
    gap: 16px;
    align-items: center;
    margin-top: 20px;
    width: 100%;
  }

  .button-column .btn {
    width: auto;
    min-width: 240px;
    max-width: 100%;
    font-size: 1.2rem;
    padding: 16px 24px;
    text-decoration: none;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    white-space: nowrap;
  }
  
  /* くくボタン用の色（さくらんぼ色） */
  .btn.accent {
    background: linear-gradient(135deg, #ff8a80, #ffbcaf);
    border-color: #ff5252;
    color: #b71c1c;
  }
  .btn.accent:active {
    transform: translateY(2px);
    border-bottom-width: 2px;
  }
</style>