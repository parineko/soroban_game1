---
// LevelSelect.astro
// カスタム設定のみのシンプル版メニュー
---

<div class="screen level-screen" data-screen="level" style="display: none;">
  <div class="panel">
    <h2 class="section-title">せってい ⚙️</h2>
    
    <div class="settings-container">
      <div class="control-group">
        <label for="custom-digits">けたすう</label>
        <div class="select-wrapper">
          <select id="custom-digits" class="custom-select">
            <option value="1">1 けた</option>
            <option value="2">2 けた</option>
            <option value="3" selected>3 けた</option>
            <option value="4">4 けた</option>
            <option value="5">5 けた</option>
          </select>
        </div>
      </div>

      <div class="control-group">
        <label for="custom-time">ひょうじ</label>
        <div class="select-wrapper">
          <select id="custom-time" class="custom-select">
            <option value="3.0">3.0 びょう</option>
            <option value="2.5">2.5 びょう</option>
            <option value="2.0" selected>2.0 びょう</option>
            <option value="1.5">1.5 びょう</option>
            <option value="1.0">1.0 びょう</option>
            <option value="0.5">0.5 びょう</option>
          </select>
        </div>
      </div>
    </div>

    <div class="action-area">
      <button class="btn start-btn" id="custom-start-btn" aria-label="ゲームスタート">
        スタート！ 🚀
      </button>
    </div>
    
    <div class="button-row">
      <button class="btn sub-btn" data-action="back-start" aria-label="ホームに戻る">もどる 🏠</button>
    </div>
  </div>
</div>

<style>
  .level-screen {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .panel {
    width: 100%;
    max-width: 500px;
    margin: 0 auto;
  }

  .section-title {
    text-align: center;
    font-size: 1.8rem;
    color: var(--text-main);
    margin-bottom: 30px;
  }

  .settings-container {
    background: #fff;
    border: 2px solid #e0d0b8;
    border-radius: 16px;
    padding: 24px;
    margin-bottom: 24px;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .control-group {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .control-group label {
    font-size: 1.2rem;
    font-weight: bold;
    color: var(--text-main);
  }

  .select-wrapper {
    position: relative;
    width: 160px;
  }

  .custom-select {
    width: 100%;
    padding: 12px;
    font-size: 1.2rem;
    border: 2px solid #d4c4b0;
    border-radius: 8px;
    background: #fff9f0;
    color: #5d4037;
    font-weight: bold;
    cursor: pointer;
    text-align: center;
    appearance: none; /* デフォルトの矢印を消す */
  }

  /* 矢印アイコンをCSSで作る */
  .select-wrapper::after {
    content: "▼";
    font-size: 0.8rem;
    color: #8b7e71;
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
  }

  .action-area {
    margin-bottom: 16px;
  }

  .btn.start-btn {
    width: 100%;
    padding: 20px;
    font-size: 1.5rem;
    background: linear-gradient(135deg, #ff9ecb, #ffb3d9);
    border: none;
    border-bottom: 4px solid #ff7bb0;
    border-radius: 12px;
    color: white;
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.1s, border-bottom 0.1s;
  }

  .btn.start-btn:active {
    transform: translateY(2px);
    border-bottom-width: 2px;
  }

  .btn.sub-btn {
    background: #f0e0c8;
    color: #5d4037;
    border: 1px solid #d4c4b0;
    padding: 10px 20px;
    border-radius: 8px;
    cursor: pointer;
  }
</style>